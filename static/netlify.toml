# The following redirect is intended for use with most SPAs that handle
# routing internally.
[[redirects]]
  from = "/tokens"
  to = "/https://raw.githubusercontent.com/dospore/tracer-balancer-token-list/master/tokens.json"
  status = 200
[[headers]]
  # Define which paths this specific [[headers]] block will cover.
  for = "/tokens"
    [headers.values]
    Access-Control-Allow-Origin = "*"
